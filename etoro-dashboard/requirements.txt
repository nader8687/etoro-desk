# pandas pinned <3: pandas 3.0 defaults string columns to the new `str` dtype,
# whose Arrow schema Streamlit's st.dataframe grid renders as a blank box
# (Performance tab tables came up empty). 2.x serializes to the well-supported
# utf8 string type.
streamlit>=1.35.0
requests>=2.31.0
pandas>=2.0.0,<3
plotly>=5.20.0
websocket-client>=1.7.0
tzdata>=2024.1
pytest>=8.0.0   # test suite (tests/) — safety-critical pure-logic coverage
